// Adiciona automaticamente a classe "active" quando a página é carregada
document.addEventListener("DOMContentLoaded", function () {
    var highlight = document.querySelector(".highlight");
    highlight.classList.add("active");
});